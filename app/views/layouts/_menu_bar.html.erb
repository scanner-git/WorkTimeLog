<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="#">Project name</a>
      <div class="nav-collapse">
        <ul class="nav">
          <li class="active"><a href="#">Home</a></li>
          <li><a href="#about">About</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
        <ul class="nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li>test</li>
              <li>test2</li>
            </ul>
          </li>
        </ul>

        <% if user_signed_in? %>
          <p class="navbar-text pull-right"><%= link_to "Abmelden", logout_path, method: :delete, class: "white" %></p>
        <% else %>
          <%= form_tag sessions_path, class: "navbar-form pull-right" do %>
            <%= text_field_tag :email, nil, class: "input-small" %>
            <%= password_field_tag :password, nil, class: "input-small"%>
            <%= submit_tag "Log in", class: "btn btn-primary" %>
          <% end %>
          <p class="navbar-text pull-right"><%= link_to "Registrieren", new_user_path, class: "white" %></p>
        <% end %>
      
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>








